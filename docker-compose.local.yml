

services:
  db:
    ports:
      - "${DB_PORT}:1433"
  
  api:
    ports:
      - "${API_PORT}:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: "${ASPNETCORE_ENVIRONMENT}"

  web:
    ports:
      - "${WEB_PORT}:80"